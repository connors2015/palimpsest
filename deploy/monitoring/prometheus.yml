# Prometheus scrape config for Sestrian nodes.
# Each node exposes /metrics (Prometheus text format) on its API port.
global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - alerts.yml

scrape_configs:
  - job_name: sestrian
    metrics_path: /metrics
    static_configs:
      # add each node's api host:port here (seed + miners)
      - targets:
          - "127.0.0.1:8090"
        labels: { role: node }
